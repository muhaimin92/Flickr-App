CREATE TABLE  PhotoTable(
    id INTEGER NOT NULL PRIMARY KEY,
    title TEXT NOT NULL,
    url TEXT NOT NULL
);

selectAll:
SELECT *
FROM PhotoTable;

insertPhoto:
INSERT OR REPLACE
INTO PhotoTable(
    id ,
    title,
    url
)VALUES (?, ?, ?);